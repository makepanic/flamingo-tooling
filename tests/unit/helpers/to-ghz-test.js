import { toGhz } from '../../../helpers/to-ghz';
import { module, test } from 'qunit';

module('Unit | Helper | to ghz');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toGhz([42]);
  assert.ok(result);
});
