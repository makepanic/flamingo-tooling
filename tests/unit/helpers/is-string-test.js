import { isString } from '../../../helpers/is-string';
import { module, test } from 'qunit';

module('Unit | Helper | is string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isString([42]);
  assert.ok(result);
});
